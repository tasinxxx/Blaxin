# Stonic Capability Matrix — BLAXIN Phase B Audit

**Method.** Every Stonic row cites Stonic's own public pages (stonicai.com, fetched 2026-09-15).
Every BLAXIN row cites real source locations and pinned tests. Source-code existence alone is
**not** treated as proof: where only unit tests (not real runtime runs) back a capability, the
status says so explicitly. No cloning — BLAXIN is an independent implementation.

**Status vocabulary:** MATCHED (implemented + verified at the Stonic floor) · SUPERIOR (beyond the
Stonic floor) · PARTIAL (real but narrower than Stonic's claim) · MISSING (no real implementation)
· UNVERIFIED (implemented; honest verification blocked by this environment).

---

## 1. Confirmed Stonic capabilities (stated on Stonic's own site)

| Capability | Stonic evidence (public) | BLAXIN implementation | Location | Verification evidence | Status | Gap | Required improvement | Verification plan |
|---|---|---|---|---|---|---|---|---|
| Natural-language commands (voice + text) | "Voice and text commands through a premium GUI"; "No memorized phrases… works out the steps" (voice-control, OpenClaw comparison) | Real intent routing: 100+ deterministic patterns → tools, unresolvable intents fall to the LLM with refusals for non-command phrasing | `server/src/router/direct.ts`, `router/commands.ts` | `direct-router.test.ts` (55+ cases incl. 5 refusal cases), `commands.test.ts`; live probe 14/14 shows DETERMINISTIC route, 0 model calls | MATCHED | — | — | Done (deterministic + live probe) |
| Voice assistant (speak, be answered) | "Hit Start AI, say the word"; voice-control page + testimonials ("love the voice assistant experice") | Browser SpeechRecognition (STT) + SpeechSynthesis (TTS), feature-detected, real state machine (`voiceState`), settings UI for voice selection | `client/src/hooks/useVoice.ts`, `components/SettingsModal.tsx` (voicetab), `hooks/useAudioFeedback.ts` | Feature-detection pinned; **physical audio round-trip unverified — no audio sink in this environment** (recorded in CONTINUATION-STATE) | UNVERIFIED (env) | Physical mic/speaker loop not verifiable here | None (code real; environment limitation documented) | Manual run on a machine with a microphone; STT transcript → directive → spoken reply |
| Screen awareness — AI sees the screen | "AI reads your screen in real-time… context other assistants simply don't have" (desktop-automation; vs-ChatGPT table) | Verified screenshots carried as `ChatMessage.images` through all four provider mappings (OpenAI parts, Anthropic blocks, Gemini inlineData, Ollama images) + OCR perception engine for grounding | `tools/screenshot.ts`, `types.ts`, `providers/messages.ts`, `utils/context-budget.ts`, `scripts/computer-use/perceive.py` | `vision-images.test.ts` (18), `vision-orchestrator.test.ts` (4, image reaches the model's message list), `vision-live.test.ts` (real X pixels → model-facing payload, PNG magic asserted) | **MATCHED** | Live-LLM vision round trip needs a provider key (env-blocked); payload contract fully proven | None beyond the env-blocked live round trip | Manual: with a key, watch the model describe a real screenshot |
| Grounded GUI control (click what it sees) | Implied by "screen awareness… and act on it" (desktop-automation) | **CLOSED LOOP PROVEN**: screenshot → OCR grounding (multi-scale, multi-PSM, word-aware ranked match) → model decision stage → smooth cursor travel → real click → observed teardown → verified gone | `scripts/computer-use/perceive.py`, `scripts/computer-use/probe-computer-use.mjs`, `tools/computer-control.ts`, `tools/web-agent.ts` | `probe-computer-use.mjs` **9/9 PASS, repeatable** (real Xvfb, real pixels, real click, real read-back); `computer-use.test.ts` wraps it in the ladder; browser grounding 6/6 real Chrome | **MATCHED** (desktop) / SUPERIOR (browser) | Vision-capable local model absent → decision stage honestly falls back to OCR grounding (recorded, never faked) | Install any Ollama vision model to light up the LLM decision stage | Re-run probe; the stage line will name the model |
| Mouse control | "Controls your entire PC" (vs-ChatGPT) | click / double-click / right-click / move / drag, X11+Wayland, pointer read-back ±2px, honest UNVERIFIED when read-back unavailable; **B4.3 polish**: real screen-bounds grounding (off-screen points REFUSED before any input is synthesized — unknown bounds stay honest, never fabricated), bounded smooth-travel interpolation (waypoints ≤12, real path between read pointer and target) | `tools/computer-control.ts` (mouse_* actions) | `tool-verification.test.ts` (mouse cases + bounds refusal/no-synthesis, waypoint interpolation/landing, single-move short path); live `desktop-live.test.ts` 5/5 on the real display | MATCHED | — | — | Done |
| Keyboard control | "controls the browser… without touching the keyboard" (voice-control) | type_text, key_press, key_combo; key charset guard; honest "events sent; receiver not verified" phrasing; **B4.3 polish**: focus awareness — the REAL active window (xdotool getactivewindow) is read before synthesizing keystrokes and named in the report, never guessed | `tools/computer-control.ts` (type_text/key_*) | Pinned honest-phrasing tests (`tool-verification.test.ts` + focused-window read and unreadable-fallback cases); receiver-verification env-blocked (host window) | MATCHED (honesty) | Keystroke receiver not verifiable in this environment | None | Manual verification on a display with a disposable focused window |
| Window / app control | "Application control without touching the keyboard"; "launching, closing, switching" | launch_app (with alive-process read-back), focus/close/minimize/maximize (window-list read-back), list_windows, get_active_window | `tools/computer-control.ts` (launch/focus/close/minimize/maximize/list) | `tool-verification.test.ts` (launch alive/dead, focus verified/failed, close verified/failed) | MATCHED | — | — | Done |
| File management | "Create, move, organize files… bulk organize downloads, find duplicates, rename by pattern" | `filesystem` tool (read/write with read-back verify, delete, rename, list) + `bulk-files` tool: organize-by-extension, batch move/copy/delete, bulk rename (prefix/suffix/replace), and a **content-hash dedupe verb** (real SHA-256 identity — size pre-grouping avoids hashing unique sizes; report mode is read-only, delete mode keeps one deterministic survivor per group and verifies every deletion from the filesystem; unreadable files honestly reported, never guessed; bounded scans only) | `tools/filesystem.ts`, `tools/bulk-files.ts`, `router/direct.ts` | `filesystem-write.test.ts` (8), `bulk-files.test.ts` (22 on the REAL filesystem: verify-every-item, silent-overwrite refusal, guard rails, cap, dedupe identity/keeper/delete-readback), `probe-bulk-dedupe.mjs` **14/14 on the real compiled dist**, router `find/delete duplicate files in <dir>` (real-dir-gated) | **MATCHED** | — | — | Done |
| Browser automation | "Open sites, search, navigate, manage tabs autonomously" | Real Chrome via CDP: open/click/type/scroll, session nav (back/forward/refresh verified by history index + reload marker), current_url/page_title/list_tabs, YouTube play/search with playback-state verification, **multi-field form fill (per-field read-back), verified form submission (real outcome), disk-verified downloads (stable-size read-back)** | `tools/browser.ts`, `tools/browser-session.ts`, `tools/cdp-browser.ts`, `tools/web-agent.ts`, `tools/verification.ts` | `browser-nav.test.ts` (14), `cdp-real-browser.test.ts` (6/6 real Chrome), live specialist probe (COMPLETED_VERIFIED from observed title), `browser-forms-downloads.test.ts` (19) + `probe-browser-forms.mjs` **10/10 on real Chrome** | **SUPERIOR** | — | — | Done |
| Multi-step missions from one instruction | "One instruction becomes a plan: scan → classify → create folders → move files → report" | Orchestrator plans → serial/parallel waves → verify; missions with per-step verification aggregation | `orchestrator/index.ts`, `utils/scheduler.ts`, `utils/missions.ts`, `orchestrator/mission-coordinator.ts` | `mission-coordinator.test.ts` (27), `missions.test.ts`, `scheduler.test.ts`; live packaged smoke showed real PLAN→ACTION→OBSERVATION→RESULT trail | MATCHED | — | — | Done |
| Human-in-the-loop safety | "Critical actions ask first. Every action is logged… a leash you hold" | Risk-tiered permission gate (LOW→CRITICAL), scopes ALWAYS_ALLOW/ALLOW_ONCE/ALLOW_TASK/ALLOW_SESSION/DENY, confirmation-required events + UI modal, stop control | `utils/permission.ts`, `utils/security.ts`, `orchestrator/index.ts`, client `ConfirmationModal.tsx` | `risk-permission.test.ts`, `permission-grants.test.ts`; BLOCKED journal lines from real gate payloads | **SUPERIOR** (graded scopes vs binary ask-first) | — | — | Done |
| Transparent logs / real-time visibility | "Every action is logged locally and visible to you" | Mission journal (bounded, persisted) derived only from real events; live HUD (activity ticker, task queue, mission panel); REST `/api/journal` | `utils/mission-journal.ts`, client `JournalPage.tsx`, `components/hud/*` | `mission-journal.test.ts` (11) + packaged-runtime smoke returning the real trail; every HUD line maps to a runtime event (fabrication audit passed) | **SUPERIOR** (persisted, inspectable, honest-status) | — | — | Done |
| System monitoring / "PC optimization" | "Monitors performance, flags resource hogs, optimizes on request"; battery/RAM/storage by voice | Real telemetry (CPU tick-delta %, RAM, disk statfs, network, uptime) + system-info tool (general/memory/cpu/disk/network/processes); terminal tool for real optimization actions; System page UI | `utils/system-telemetry.ts`, `tools/system-info.ts`, `tools/terminal.ts`, client `pages/SystemPage.tsx` | `system-telemetry.test.ts`, `terminal-verification.test.ts`; UI polls only when mounted | PARTIAL | No battery sensor read (Linux APM path is environment-specific); "optimization" is real terminal work, not a canned optimizer | Acceptable: honest real data vs Stonic's unspecified "optimization"; battery read is a small 10× item | Live: battery via /sys/class/power_supply when present |
| Volume control | "control volume and settings by asking" (voice-control) | `system-audio` tool: get/set/mute/unmute via wpctl with get→set→get read-back verification; deterministic router phrases ("volume", "set volume to 40", "mute") | `tools/system-audio.ts`, `router/direct.ts` | `system-audio.test.ts` (9: read-back verified, silent-failure caught, unavailability honest), `system-audio-live.test.ts` **ran LIVE** on the real PipeWire sink (set→fresh-instance get→restore), router cases in `direct-router.test.ts` (15) | **MATCHED** | — | — | Done |
| Messaging (WhatsApp) | "WhatsApp integration built directly into the interface"; "Dictate and send WhatsApp messages" | None — and deliberately out of scope (closed-platform automation against ToS; BLAXIN's distributed Body/Brain covers remote messaging surfaces) | — (absent) | Absent (checked repo-wide) | MISSING (documented) | Out of product scope; not a Stonic floor for a privacy-first desktop agent | Record as a documented non-goal | — |
| Memory ("it remembers you") | "Memory — it remembers you: your name, your work, your preferences" (homepage) | Layered memory with provenance: failures, environment, episodes, procedures; secret-refusing redaction; orchestrator memory injection + selection events; REST inspection; **CLOSED LEARNING LOOP (B6)** — fully VERIFIED multi-step runs promote reusable procedures (store-side honesty gates: sensitive content refused, bounded steps), the advisor surfaces them to similar objectives ("never replay blindly" framing), and procedures the advisor selected lose confidence on real run failures (auto-rollback at the store threshold, so stale procedures stop surfacing) | `memory/layers.ts`, `memory/advisor.ts`, `utils/memory.ts`, `orchestrator/index.ts` | `memory-layers.test.ts` (21), `memory-orchestrator.test.ts`, `memory-readback.test.ts`, `memory-context.test.ts`, `memory-procedure-learning.test.ts` (8: promotion, no-trivial-recipes, failed-runs-never-promote, by-id failure accounting, success-no-accounting, END-TO-END auto-rollback + advisor stop-surfacing, future-objective reuse, throwing-store safety) | **SUPERIOR** (provenance-tracked, layered, redaction-guarded + a verified LEARNING loop vs a flat persona memory) | — | — | Done |
| Skills | "Skills — the real abilities it uses" | 39-skill runtime library: SKILL.md parse, scored selection against the objective, budgeted context composition, audit reasons | `skills/registry.ts`, `.agents/skills/` | `__tests__/skills/*` suites; selection reasons inspectable | MATCHED | — | — | Done |
| Specialist agents | "Four resident agents… real AI agents with desks, roles, and live status" | Bounded objectives delegated to specialists (browser/files/terminal roles derived from the tool that really ran) with action/recovery/replan budgets, deadline, honest settlement; multi-specialist mission coordination | `orchestrator/specialist.ts`, `orchestrator/mission-coordinator.ts` | `specialist-ownership.test.ts` (24), `browser-specialist.test.ts` (14), `mission-coordinator.test.ts` (27); live verified-specialist proof (journal DELEGATED→ACTION→OBSERVATION→VERIFICATION→RESULT) | **SUPERIOR** (bounded ownership + honest budgets vs avatar desks) | — | — | Done |
| Multi-agent collaboration / delegation | "Agents use MCP servers and WebSocket protocols to delegate sub-tasks" | Mission coordinator ingests real specialist evidence, expands {{evidence}} templates, shares bounded context, cancels propagation | `orchestrator/mission-coordinator.ts` | `mission-coordinator.test.ts` (27) | MATCHED | — | — | Done |
| Planning + recovery/re-plan | "plans the steps itself, adapts to what it finds" | Deterministic failure classification → bounded recovery ladder (retry/backoff, reobserve, alternate path) → one re-plan → Brain escalation only post-exhaustion | `orchestrator/recovery-policy.ts`, `orchestrator/index.ts` | `recovery-policy.test.ts` (32), `deterministic-recovery.test.ts` (11), `journal-recovery.test.ts` (4) | **SUPERIOR** (deterministic, budgeted, zero-model recovery) | — | — | Done |
| Local-first privacy / zero telemetry | "Processed locally… no behavioral mining… files never leave" | Local data dir (`.blaxin-state`), no telemetry, local models via Ollama + local resource inventory, Brain/Body split keeps execution local | `utils/paths.ts`, `models/*`, `distributed/*` | `models/*` suites; real health-check-gated model states (never a READY without a real round trip) | **SUPERIOR** (local *execution*, not just local processing) | — | — | Done |
| Cinematic JARVIS-style interface | "cinematic, voice-controlled sci-fi desktop… three full themes" | Full HUD: boot overlay, particle canvas, Jarvis panel with real executive phases, agency/mission/memory/network/security panels, two themes, reduced-motion + a11y support | `client/src/components/hud/*`, `client/src/theme/*` | E2E 8/8 over real backend; HUD fabrication audit; real WS/REST data only | MATCHED | — | — | Done |
| Mobile remote / phone pairing | "pair your phone once, then send commands… from anywhere" (v1.0.57) | Body/Brain remote-brain: authenticated pairing, capability validation, wss transport | `distributed/pairing.ts`, `distributed/remote-brain.ts`, `docs/distributed-brain.md` | `distributed/*` suites (pairing/protocol/handshake); test-tls real-socket pairing tests | MATCHED | — | — | Done |
| World monitor (globe, map, headlines) | "A live 3D globe… 2D command map… today's headlines" | Network hub panel (real local network data) + web search; no globe/map | `components/hud/NetworkHubPanel.tsx`, `tools/search.ts` | Panel fed by real events; search tool real fetches | PARTIAL | Decorative world feed has no BLAXIN equivalent — deliberately: real data only, no widget that pretends | Optional 10×: a real network-topology view from actual telemetry | — |
| Personality / "Soul" tuning | "Soul — tune its personality" | System prompt boundary + settings; JARVIS persona in prompts | `orchestrator/index.ts` (system prompt), `SettingsModal.tsx` | `prompt-boundary.test.ts` pins the boundary | PARTIAL | No user-facing persona editor | 10× item, not parity | — |

## 2. Uncertain / unknown Stonic capabilities (not confirmed on their site)

Stonic is closed-source; the following cannot be confirmed from public material and are tracked
as unknowns, not floors: on-device model identity ("Both AI models — a fast one… a heavy one" —
hosted, per their FAQ: "the AI runs on our servers"); actual screen-understanding depth (claimed
but never demonstrated with evidence — screenshots or transcript proof); computer-use loops
(claimed "sees and acts", no public demonstration of closed-loop pixel→action verification);
whether actions are verified at all (their transparency claim covers logging, not verification).
**BLAXIN therefore does NOT need parity against unproven claims — only against the confirmed
rows above.**

## 3. Phase B implementation from this audit

The single highest-value parity gap found: **screen awareness is half-real**. The screenshot tool
verifies real pixels, but `settleResult()` reduced every tool result to text, so the model could
never actually SEE the screen it was told to act on. Stonic's core claim (AI reads the screen and
acts) had no BLAXIN equivalent end-to-end.

Implemented this phase (independent implementation, no cloning):
1. `ChatMessage.images` (bounded array of `{ mimeType, base64 }`) on the uniform message type.
2. `settleResult` carries a **bounded** image from screenshot tool results (single screenshot per
   step, hard size cap, stripped from anything persisted via `budgetImages()`).
3. Provider mappers translate images per wire format:
   - OpenAI-compatible: `tool` result → `user` content parts (`image_url` data URL) — the format
     the OpenAI Chat Completions API requires for images after a tool call.
   - Anthropic: `tool_result` content blocks gain `image` source blocks (base64).
   - Gemini: `functionResponse` parts gain `inlineData` parts.
   - Ollama: `images` array on the tool message (Ollama's native multimodal format).

## 4. Remaining tracked gaps (post-audit backlog, updated after Objectives 1–3)

| Gap | Status | Class |
|---|---|---|
| Volume control | **CLOSED** — `system-audio` implemented + live-verified on PipeWire | Parity gap closed |
| Bulk file verbs | **CLOSED** — `bulk-files` implemented + verified on the real filesystem | Parity gap closed |
| Closed-loop computer use | **PROVEN** — 9/9 repeatable probe on real pixels; LLM decision stage honest-fallbacks until a vision model is installed | Parity loop proven |
| Live-LLM vision round trip | Env-blocked (no provider key; no local vision-capable model) — payload contract fully proven | Environment limitation |
| Voice physical round-trip | Env-blocked (documented) | Environment limitation |
| Battery telemetry | **CLOSED** — `/sys/class/power_supply` + X11/window/audio/process sensors live on the system panel; 12 pinned contract tests (`system-awareness.test.ts`) | 10× closed |
| Adaptive model routing | **CLOSED** — capability-aware router (real `/api/tags` capability data, honest BLOCK, bounded fallback + reliability history); live probe **16/16 PASS** on the real runtime incl. real local inference, the honest vision block, and a live bounded fallback after a real NETWORK_ERROR | 10× closed |
| Content-hash dedupe verb | **CLOSED** — real SHA-256 content identity in `bulk-files dedupe` (report/delete modes), verified on the real filesystem + real compiled dist (probe 14/14); MissionPanel surfaces the server-derived bulk aggregates | 10× closed |
| Browser form-fill + submit + downloads | **CLOSED** — `blaxin_web fill_form` (per-field read-back verification, ≤20 fields, inputs/selects/checkboxes), `form_submit` (REAL outcome: navigation or the page's own confirmation — "clicked submit" is never evidence), `download` (CDP download routing + filesystem read-back: SUCCESS only when the file REALLY exists with a stable non-zero size); deterministic router routes bare submit + single-target downloads; probe **10/10 PASS on real Chrome** (real navigation verified, 4/4 fields, honest validation-blocked submit, 404 download honestly fails, grounding refuses ghosts) | 10× closed |
| Memory-driven procedure learning | **CLOSED** — verified multi-step runs become reusable procedures; advisor-selected procedures are failure-accounted by REAL record id; repeated failures trip auto-rollback and the advisor stops surfacing them (8 orchestrator-side pinned tests, END-TO-END with the real store) | 10× closed |
| Persona editor / world monitor | Non-goals for parity; 10× candidates | Optional |
